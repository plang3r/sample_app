<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= ajax_will_paginate @users %>

<ul class="users">
	<%= render @users %>
</ul>

<%= ajax_will_paginate @users %>
<%= javascript_include_tag "user_paginate.js" %>
